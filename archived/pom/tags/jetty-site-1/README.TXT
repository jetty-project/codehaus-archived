To release a new jetty website follow the normal maven release process.

Be sure to check the values of the jetty6.version and jetty7.version properties so they point
to the correct version of their respective jetty version so the correct javadoc and xref artifacts
are brought into the build.